var ADMIN_PERMISSIONS=Object.freeze({ADMIN:['DASHBOARD','USERS','SCHOOLS','SEASONS','QUESTIONS','QUIZ','LEADERBOARD','WINNERS','REWARDS','MATERIALS','ANNOUNCEMENTS','FRAUD','REPORTS'],SUPERADMIN:['*']});
var AdminControlService=(function(){
  var roles=['ADMIN','SUPERADMIN'];
  function access_(token,permission,superOnly){var auth=requireRole(token,superOnly?['SUPERADMIN']:roles),allowed=ADMIN_PERMISSIONS[auth.user.Role]||[];if(allowed.indexOf('*')<0&&allowed.indexOf(permission)<0)throw new AppError('Anda tidak memiliki izin.','FORBIDDEN');return auth;}
  function page_(rows,input,fields){input=input||{};var search=sanitizeText_(input.search||'',120,'Pencarian').toLowerCase(),filters=input.filters||{},sort=fields.indexOf(input.sortBy)>=0?input.sortBy:fields[0],direction=String(input.sortDir).toLowerCase()==='asc'?1:-1,page=Math.max(parseInt(input.page,10)||1,1),limit=Math.min(Math.max(parseInt(input.limit,10)||20,1),100);if(search)rows=rows.filter(function(row){return fields.some(function(field){return String(row[field]||'').toLowerCase().indexOf(search)>=0;});});Object.keys(filters).forEach(function(field){if(fields.indexOf(field)<0||filters[field]===''||filters[field]===undefined)return;rows=rows.filter(function(row){return String(row[field])===String(filters[field]);});});rows.sort(function(a,b){var av=a[sort]||'',bv=b[sort]||'';if(typeof av==='number'||typeof bv==='number')return(Number(av)-Number(bv))*direction;return String(av).localeCompare(String(bv))*direction;});return{items:rows.slice((page-1)*limit,page*limit),pagination:{page:page,limit:limit,total:rows.length,pages:Math.ceil(rows.length/limit)}};}
  function audit_(auth,action,entity,id,description){logActivity_(auth.user.UserID,action,entity,id,description,'ADMIN_PANEL');}
  function dashboard(token){access_(token,'DASHBOARD');var users=DatabaseService.getAllRows('Users'),schools=DatabaseService.getAllRows('Schools'),seasons=DatabaseService.getAllRows('Seasons'),sessions=DatabaseService.getAllRows('QuizSessions'),points=DatabaseService.getAllRows('PointTransactions'),fraud=DatabaseService.getAllRows('FraudLogs'),winners=DatabaseService.getAllRows('Winners'),active=seasons.filter(function(x){return x.Status==='ACTIVE';})[0]||null,seasonId=active?active.SeasonID:'',validPoints=points.filter(function(x){return x.Status==='VALID'&&(!seasonId||x.SeasonID===seasonId);}),students=users.filter(function(x){return x.Role==='STUDENT';}),completed=sessions.filter(function(x){return x.Status==='COMPLETED'&&(!seasonId||x.SeasonID===seasonId);});var registrations={},schoolCount={},scores={LOW:0,MEDIUM:0,HIGH:0};students.forEach(function(x){var d=String(x.CreatedAt||'').slice(0,7)||'Unknown';registrations[d]=(registrations[d]||0)+1;schoolCount[x.SchoolID]=(schoolCount[x.SchoolID]||0)+1;});completed.forEach(function(x){var score=Number(x.Score||0);scores[score<50?'LOW':score<80?'MEDIUM':'HIGH']++;});var schoolMap={};schools.forEach(function(x){schoolMap[x.SchoolID]=x.SchoolName;});var latest=winners.filter(function(x){return x.Status==='PUBLISHED';}).sort(function(a,b){return String(b.SelectedAt).localeCompare(String(a.SelectedAt));})[0]||null;return successResponse({stats:{totalParticipants:students.length,activeParticipants:students.filter(function(x){return x.Status==='ACTIVE';}).length,totalSchools:schools.filter(function(x){return x.Status==='ACTIVE';}).length,activeSeason:active?active.Name:'-',quizCompleted:completed.length,totalSeasonPoint:validPoints.reduce(function(s,x){return s+Number(x.Point||0);},0,0),participantsReview:fraud.filter(function(x){return x.Status==='REVIEW'||x.Status==='HIGH_RISK';}).length,latestWinner:latest?latest.UserID:'-'},charts:{registrations:Object.keys(registrations).sort().map(function(k){return{label:k,value:registrations[k]};}),schools:Object.keys(schoolCount).map(function(k){return{label:schoolMap[k]||k,value:schoolCount[k]};}).sort(function(a,b){return b.value-a.value;}).slice(0,10),scoreDistribution:Object.keys(scores).map(function(k){return{label:k,value:scores[k]};}),quizCompletion:[{label:'Selesai',value:completed.length},{label:'Belum selesai',value:sessions.filter(function(x){return x.Status!=='COMPLETED'&&(!seasonId||x.SeasonID===seasonId);}).length}],pointDistribution:distribution_(validPoints)},recentActivity:DatabaseService.getAllRows('ActivityLogs').sort(function(a,b){return String(b.CreatedAt).localeCompare(String(a.CreatedAt));}).slice(0,8)},'Dashboard berhasil dimuat.');}
  function distribution_(points){var totals={};points.forEach(function(x){totals[x.UserID]=(totals[x.UserID]||0)+Number(x.Point||0);});var buckets={'0-99':0,'100-499':0,'500+':0};Object.keys(totals).forEach(function(k){var n=totals[k];buckets[n<100?'0-99':n<500?'100-499':'500+']++;});return Object.keys(buckets).map(function(k){return{label:k,value:buckets[k]};});}
  function users(token,input){access_(token,'USERS');var schools={};DatabaseService.getAllRows('Schools').forEach(function(s){schools[s.SchoolID]=s;});var all=DatabaseService.getAllRows('Users').filter(function(u){return u.Role==='STUDENT';}),ranked=all.slice().sort(function(a,b){return Number(b.TotalPointCache||0)-Number(a.TotalPointCache||0);}),ranks={};ranked.forEach(function(u,i){ranks[u.UserID]=i+1;});var rows=all.map(function(u){return{UserID:u.UserID,Name:u.Name,NIS:u.NIS,SchoolID:u.SchoolID,SchoolName:schools[u.SchoolID]?schools[u.SchoolID].SchoolName:'',District:schools[u.SchoolID]?schools[u.SchoolID].District:'',Class:u.Class,BirthDate:u.BirthDate,WhatsApp:u.WhatsApp,Email:u.Email,Status:u.Status,Point:Number(u.TotalPointCache||0),Ranking:ranks[u.UserID],CreatedAt:u.CreatedAt,LastLogin:u.LastLogin};});return successResponse(page_(rows,input,['CreatedAt','Name','NIS','Email','SchoolID','SchoolName','District','Class','Status','Point','Ranking']),'Peserta berhasil dimuat.');}
  function userDetail(token,input){access_(token,'USERS');var id=sanitizeText_(input.userId,40,'UserID'),u=DatabaseService.findById('Users','UserID',id);if(!u||u.Role!=='STUDENT')throw new AppError('Peserta tidak ditemukan.','USER_NOT_FOUND');var sessions=DatabaseService.findMany('QuizSessions','UserID',id),completed=sessions.filter(function(x){return x.Status==='COMPLETED';}),tx=DatabaseService.findMany('PointTransactions','UserID',id),ach=DatabaseService.findMany('UserAchievements','UserID',id),logs=DatabaseService.findMany('ActivityLogs','UserID',id);return successResponse({user:{UserID:u.UserID,Name:u.Name,NIS:u.NIS,SchoolID:u.SchoolID,Class:u.Class,BirthDate:u.BirthDate,WhatsApp:u.WhatsApp,Email:u.Email,Status:u.Status,FraudScore:Number(u.FraudScore||0)},stats:{totalPoint:Number(u.TotalPointCache||0),quizCompleted:completed.length,correctAnswers:completed.reduce(function(s,x){return s+Number(x.Correct||0);},0),averageScore:completed.length?Math.round(completed.reduce(function(s,x){return s+Number(x.Score||0);},0)/completed.length):0},quiz:sessions,points:tx,achievements:ach,activity:logs.slice(-50)},'Detail peserta berhasil dimuat.');}
  function updateUser(token,input){var auth=access_(token,'USERS'),id=sanitizeText_(input.userId,40,'UserID'),u=DatabaseService.findById('Users','UserID',id);if(!u||u.Role!=='STUDENT')throw new AppError('Peserta tidak ditemukan.','USER_NOT_FOUND');var changes={UpdatedAt:nowIso_()};[['name','Name',120],['nis','NIS',20],['schoolId','SchoolID',40],['class','Class',40],['birthDate','BirthDate',20],['whatsapp','WhatsApp',24],['email','Email',160]].forEach(function(x){if(input[x[0]]!==undefined)changes[x[1]]=rejectFormula_(input[x[0]],x[2],x[1]);});DatabaseService.updateById('Users','UserID',id,changes);audit_(auth,'ADMIN_UPDATE_USER','User',id,'Memperbarui peserta');return successResponse({UserID:id},'Peserta berhasil diperbarui.');}
  function userStatus(token,input){var auth=access_(token,'USERS'),id=sanitizeText_(input.userId,40,'UserID'),status=sanitizeText_(input.status,20,'Status').toUpperCase(),reason=rejectFormula_(input.reason||'',500,'Alasan'),u=DatabaseService.findById('Users','UserID',id);ValidationService.validateEnum(status,DATABASE_ENUMS.UserStatus,'Status');if(!u||u.Role!=='STUDENT')throw new AppError('Peserta tidak ditemukan.','USER_NOT_FOUND');if((status==='SUSPENDED'||status==='BLOCKED')&&!reason)throw new AppError('Alasan wajib diisi.','VALIDATION_ERROR');DatabaseService.updateById('Users','UserID',id,{Status:status,UpdatedAt:nowIso_()});audit_(auth,'ADMIN_'+status+'_USER','User',id,reason||'Mengubah status menjadi '+status);return successResponse({UserID:id,Status:status},'Status peserta berhasil diperbarui.');}
  function resetPassword(token,input){var auth=access_(token,'USERS'),id=sanitizeText_(input.userId,40,'UserID'),password=String(input.temporaryPassword||'');if(password.length<8||password.length>128)throw new AppError('Password sementara harus 8-128 karakter.','VALIDATION_ERROR');if(!DatabaseService.findById('Users','UserID',id))throw new AppError('Pengguna tidak ditemukan.','USER_NOT_FOUND');var salt=generateSalt();DatabaseService.updateById('Users','UserID',id,{PasswordHash:hashPassword(password,salt),PasswordSalt:salt,UpdatedAt:nowIso_()});audit_(auth,'ADMIN_RESET_PASSWORD','User',id,'Reset password pengguna');return successResponse({UserID:id},'Password sementara berhasil disimpan.');}
  function list_(token,input,permission,sheet,fields){access_(token,permission);return successResponse(page_(DatabaseService.getAllRows(sheet),input,fields),'Data berhasil dimuat.');}
  function schools(token,input){var users=DatabaseService.getAllRows('Users'),count={};users.forEach(function(u){count[u.SchoolID]=(count[u.SchoolID]||0)+1;});access_(token,'SCHOOLS');var rows=DatabaseService.getAllRows('Schools').map(function(s){var out={};Object.keys(s).forEach(function(k){out[k]=s[k];});out.ParticipantCount=count[s.SchoolID]||0;return out;});return successResponse(page_(rows,input,['CreatedAt','SchoolName','NPSN','Type','District','Status','ParticipantCount']),'Sekolah berhasil dimuat.');}
  function saveSchool(token,input){var auth=access_(token,'SCHOOLS');if(input.schoolId){var r=AdminService.updateSchool(token,input);audit_(auth,'ADMIN_UPDATE_SCHOOL','School',input.schoolId,'Memperbarui sekolah');return r;}var created=AdminService.createSchool(token,input);return created;}
  function schoolStatus(token,input){var auth=access_(token,'SCHOOLS'),id=sanitizeText_(input.schoolId,40,'SchoolID'),status=sanitizeText_(input.status,20,'Status');ValidationService.validateEnum(status,DATABASE_ENUMS.SchoolStatus,'Status');if(!DatabaseService.updateById('Schools','SchoolID',id,{Status:status,UpdatedAt:nowIso_()}))throw new AppError('Sekolah tidak ditemukan.','SCHOOL_NOT_FOUND');audit_(auth,'ADMIN_'+(status==='ACTIVE'?'ACTIVATE':'DISABLE')+'_SCHOOL','School',id,'Mengubah status sekolah');return successResponse({SchoolID:id,Status:status},'Status sekolah berhasil diperbarui.');}
  function seasons(token,input){return list_(token,input,'SEASONS','Seasons',['CreatedAt','Name','Theme','StartDate','EndDate','Status']);}
  function validateTransition_(from,to){var flow={DRAFT:['SCHEDULED'],SCHEDULED:['DRAFT','ACTIVE'],ACTIVE:['FINISHED'],FINISHED:['ARCHIVED'],ARCHIVED:[]};if(from===to)return;if((flow[from]||[]).indexOf(to)<0)throw new AppError('Transisi season '+from+' ke '+to+' tidak diizinkan.','INVALID_SEASON_TRANSITION');}
  function updateSeason(token,input){var auth=access_(token,'SEASONS'),id=sanitizeText_(input.seasonId,40,'SeasonID'),s=DatabaseService.findById('Seasons','SeasonID',id);if(!s)throw new AppError('Season tidak ditemukan.','SEASON_NOT_FOUND');var changes={UpdatedAt:nowIso_()};[['name','Name',160],['theme','Theme',160],['description','Description',2000],['startDate','StartDate',30],['endDate','EndDate',30],['reward','Reward',200]].forEach(function(x){if(input[x[0]]!==undefined)changes[x[1]]=rejectFormula_(input[x[0]],x[2],x[1]);});[['questionCount','QuestionCount'],['pointPerQuestion','PointPerQuestion'],['maxAttempt','MaxAttempt'],['quizDuration','QuizDuration']].forEach(function(x){if(input[x[0]]!==undefined){var n=Number(input[x[0]]);if(!Number.isInteger(n)||n<=0)throw new AppError(x[1]+' harus positif.','VALIDATION_ERROR');changes[x[1]]=n;}});if(input.showExplanation!==undefined)changes.ShowExplanation=Boolean(input.showExplanation);var start=changes.StartDate||s.StartDate,end=changes.EndDate||s.EndDate;if(new Date(end)<=new Date(start))throw new AppError('Tanggal selesai harus setelah tanggal mulai.','VALIDATION_ERROR');DatabaseService.updateById('Seasons','SeasonID',id,changes);audit_(auth,'ADMIN_UPDATE_SEASON','Season',id,'Memperbarui season');return successResponse({SeasonID:id},'Season berhasil diperbarui.');}
  function seasonStatus(token,input){var auth=access_(token,'SEASONS'),id=sanitizeText_(input.seasonId,40,'SeasonID'),status=sanitizeText_(input.status,20,'Status'),s=DatabaseService.findById('Seasons','SeasonID',id);if(!s)throw new AppError('Season tidak ditemukan.','SEASON_NOT_FOUND');validateTransition_(s.Status,status);if(status==='ACTIVE'&&DatabaseService.getAllRows('Seasons').some(function(x){return x.SeasonID!==id&&x.Status==='ACTIVE';}))throw new AppError('Masih ada season ACTIVE. Selesaikan season tersebut terlebih dahulu.','ACTIVE_SEASON_EXISTS');DatabaseService.updateById('Seasons','SeasonID',id,{Status:status,UpdatedAt:nowIso_()});audit_(auth,'ADMIN_'+status+'_SEASON','Season',id,'Mengubah status season');return successResponse({SeasonID:id,Status:status},'Status season berhasil diperbarui.');}
  function duplicateSeason(token,input){var auth=access_(token,'SEASONS'),s=DatabaseService.findById('Seasons','SeasonID',sanitizeText_(input.seasonId,40,'SeasonID'));if(!s)throw new AppError('Season tidak ditemukan.','SEASON_NOT_FOUND');var copy={};DATABASE_SCHEMA.Seasons.forEach(function(k){copy[k]=s[k];});copy.SeasonID=generateId('SEA');copy.Name=rejectFormula_(input.name||s.Name+' (Salinan)',160,'Nama');copy.Status='DRAFT';copy.CreatedAt=copy.UpdatedAt=nowIso_();DatabaseService.insert('Seasons',copy);audit_(auth,'ADMIN_DUPLICATE_SEASON','Season',copy.SeasonID,'Menduplikasi season');return successResponse(copy,'Season berhasil diduplikasi.');}
  function questions(token,input){return list_(token,input,'QUESTIONS','Questions',['CreatedAt','UpdatedAt','Question','Category','Difficulty','Point','Status']);}
  function updateQuestion(token,input){var auth=access_(token,'QUESTIONS'),id=sanitizeText_(input.questionId,40,'QuestionID'),q=DatabaseService.findById('Questions','QuestionID',id);if(!q)throw new AppError('Soal tidak ditemukan.','QUESTION_NOT_FOUND');var map={category:['Category',100],question:['Question',2000],optionA:['OptionA',500],optionB:['OptionB',500],optionC:['OptionC',500],optionD:['OptionD',500],explanation:['Explanation',3000]},changes={UpdatedAt:nowIso_()};Object.keys(map).forEach(function(k){if(input[k]!==undefined)changes[map[k][0]]=rejectFormula_(input[k],map[k][1],map[k][0]);});if(input.correctAnswer!==undefined){var a=String(input.correctAnswer).toUpperCase();ValidationService.validateEnum(a,['A','B','C','D'],'Jawaban');changes.CorrectAnswer=a;}if(input.difficulty!==undefined){ValidationService.validateEnum(input.difficulty,DATABASE_ENUMS.Difficulty,'Difficulty');changes.Difficulty=input.difficulty;}if(input.point!==undefined){var p=Number(input.point);if(!Number.isInteger(p)||p<=0)throw new AppError('Point harus positif.','VALIDATION_ERROR');changes.Point=p;}DatabaseService.updateById('Questions','QuestionID',id,changes);invalidateQuestionCache_();audit_(auth,'ADMIN_UPDATE_QUESTION','Question',id,'Memperbarui soal');return successResponse({QuestionID:id},'Soal berhasil diperbarui.');}
  function questionStatus(token,input){var auth=access_(token,'QUESTIONS'),ids=input.questionIds||[input.questionId],status=sanitizeText_(input.status,20,'Status');ValidationService.validateEnum(status,DATABASE_ENUMS.QuestionStatus,'Status');ids.forEach(function(id){DatabaseService.updateById('Questions','QuestionID',sanitizeText_(id,40,'QuestionID'),{Status:status,UpdatedAt:nowIso_()});});invalidateQuestionCache_();audit_(auth,'ADMIN_'+status+'_QUESTION','Question',ids.join(','),'Mengubah status soal');return successResponse({count:ids.length,Status:status},'Status soal berhasil diperbarui.');}
  function quizSessions(token,input){access_(token,'QUIZ');input=input||{};var rows=DatabaseService.getAllRows('QuizSessions'),seasonId=sanitizeText_((input.filters||{}).SeasonID||'',40,'SeasonID'),selected=seasonId?rows.filter(function(x){return x.SeasonID===seasonId;}):rows,completed=selected.filter(function(x){return x.Status==='COMPLETED';}),users={};selected.forEach(function(x){users[x.UserID]=true;});return successResponse({summary:{participants:Object.keys(users).length,started:selected.filter(function(x){return x.Status==='STARTED';}).length,completed:completed.length,expired:selected.filter(function(x){return x.Status==='EXPIRED';}).length,averageScore:completed.length?Math.round(completed.reduce(function(s,x){return s+Number(x.Score||0);},0)/completed.length):0,averagePoint:completed.length?Math.round(completed.reduce(function(s,x){return s+Number(x.TotalPoint||0);},0)/completed.length):0},table:page_(selected,input,['CreatedAt','UserID','SeasonID','StartedAt','FinishedAt','Score','TotalPoint','Status'])},'Quiz session berhasil dimuat.');}
  function adjustPoint(token,input){var auth=access_(token,'REPORTS',true),userId=sanitizeText_(input.userId,40,'UserID'),seasonId=sanitizeText_(input.seasonId,40,'SeasonID'),point=Number(input.point),reason=rejectFormula_(input.reason||'',500,'Alasan');if(!DatabaseService.findById('Users','UserID',userId)||!DatabaseService.findById('Seasons','SeasonID',seasonId))throw new AppError('User atau season tidak ditemukan.','NOT_FOUND');if(!Number.isInteger(point)||point===0||!reason)throw new AppError('Point non-zero dan alasan wajib diisi.','VALIDATION_ERROR');var row={PointID:generateId('PNT'),UserID:userId,SeasonID:seasonId,SourceType:'ADMIN',SourceID:'',Point:point,Description:reason,Status:'VALID',CreatedAt:nowIso_(),CreatedBy:auth.user.UserID};row.SourceID=row.PointID;DatabaseService.insert('PointTransactions',row);PointService.recalculateUserTotalPoint(userId);invalidateLeaderboardCache_(seasonId);audit_(auth,'ADMIN_ADJUST_POINT','PointTransaction',row.PointID,reason);return successResponse(row,'Penyesuaian point berhasil.');}
  function contentList(token,input,kind){var sheet=kind==='material'?'Materials':'Announcements',permission=kind==='material'?'MATERIALS':'ANNOUNCEMENTS';return list_(token,input,permission,sheet,['CreatedAt','UpdatedAt','Title','Category','Type','Status','StartDate','EndDate']);}
  function saveContent(token,input,kind){var permission=kind==='material'?'MATERIALS':'ANNOUNCEMENTS',auth=access_(token,permission),sheet=kind==='material'?'Materials':'Announcements',idField=kind==='material'?'MaterialID':'AnnouncementID',id=input[kind+'Id'],now=nowIso_(),row;if(kind==='material'){var status=sanitizeText_(input.status||'DRAFT',20,'Status');ValidationService.validateEnum(status,['DRAFT','PUBLISHED','ARCHIVED'],'Status');row={Title:rejectFormula_(input.title,200,'Judul'),Category:rejectFormula_(input.category||'',100,'Kategori'),Thumbnail:sanitizeText_(input.thumbnail||'',500,'Thumbnail'),Content:sanitizeContent_(input.content||''),VideoURL:sanitizeText_(input.videoUrl||'',500,'Video URL'),Status:status,PublishedAt:status==='PUBLISHED'?now:'',UpdatedAt:now};}else{var astatus=sanitizeText_(input.status||'DRAFT',20,'Status');ValidationService.validateEnum(astatus,['DRAFT','ACTIVE','INACTIVE','ARCHIVED'],'Status');ValidationService.validateEnum(sanitizeText_(input.type||'INFO',20,'Type'),['INFO','IMPORTANT','WINNER','MAINTENANCE'],'Type');row={Title:rejectFormula_(input.title,200,'Judul'),Content:sanitizeContent_(input.content||''),Type:input.type||'INFO',Status:astatus,StartDate:sanitizeText_(input.startDate||'',30,'Mulai'),EndDate:sanitizeText_(input.endDate||'',30,'Selesai'),UpdatedAt:now};}ValidationService.validateRequired(row.Title,'Judul');if(id){if(!DatabaseService.updateById(sheet,idField,id,row))throw new AppError('Data tidak ditemukan.','NOT_FOUND');}else{id=generateId(kind==='material'?'MAT':'ANN');row[idField]=id;row.CreatedAt=now;DatabaseService.insert(sheet,row);}audit_(auth,'ADMIN_'+(input[kind+'Id']?'UPDATE':'CREATE')+'_'+kind.toUpperCase(),kind,id,'Menyimpan '+kind);return successResponse(row,'Data berhasil disimpan.');}
  function sanitizeContent_(value){var text=String(value||'');if(text.length>20000)throw new AppError('Konten terlalu panjang.','VALIDATION_ERROR');return text.replace(/<\/?(script|style|iframe)[^>]*>/gi,'').replace(/\son\w+\s*=\s*(["']).*?\1/gi,'').replace(/javascript:/gi,'');}
  function fraud(token,input){access_(token,'FRAUD');var users={},schools={};DatabaseService.getAllRows('Users').forEach(function(x){users[x.UserID]=x;});DatabaseService.getAllRows('Schools').forEach(function(x){schools[x.SchoolID]=x.SchoolName;});var rows=DatabaseService.getAllRows('FraudLogs').map(function(x){var u=users[x.UserID]||{},r={};Object.keys(x).forEach(function(k){r[k]=x[k];});r.Name=u.Name||'';r.SchoolName=schools[u.SchoolID]||'';r.FraudScore=Number(u.FraudScore||x.Score||0);return r;});return successResponse(page_(rows,input,['CreatedAt','UserID','Name','SchoolName','SessionID','Type','Score','FraudScore','Status','Description']),'Fraud log berhasil dimuat.');}
  function reviewFraud(token,input){var auth=access_(token,'FRAUD'),id=sanitizeText_(input.fraudLogId,40,'FraudLogID'),status=sanitizeText_(input.status,20,'Status');ValidationService.validateEnum(status,['REVIEWED','RESOLVED','CONFIRMED'],'Status');if(!DatabaseService.updateById('FraudLogs','FraudLogID',id,{Status:status,ReviewedAt:nowIso_(),ReviewedBy:auth.user.UserID}))throw new AppError('Fraud log tidak ditemukan.','NOT_FOUND');audit_(auth,'ADMIN_REVIEW_FRAUD','FraudLog',id,'Review fraud: '+status);return successResponse({FraudLogID:id,Status:status},'Review fraud berhasil disimpan.');}
  function activity(token,input){return list_(token,input,'SETTINGS','ActivityLogs',['CreatedAt','UserID','Action','Entity','Description'],true);}
  function rewards(token,input){return list_(token,input,'REWARDS','Rewards',['CreatedAt','UpdatedAt','SeasonID','WinnerID','RewardName','Nominal','Provider','Status','SentAt']);}
  function settings(token){access_(token,'SETTINGS',true);var allowed=DEFAULT_SETTINGS.map(function(x){return x.Key;});return successResponse(DatabaseService.getAllRows('Settings').filter(function(x){return allowed.indexOf(x.Key)>=0;}),'Settings berhasil dimuat.');}
  function updateSetting(token,input){var auth=access_(token,'SETTINGS',true),key=sanitizeText_(input.key,60,'Key'),definition=DEFAULT_SETTINGS.filter(function(x){return x.Key===key;})[0];if(!definition)throw new AppError('Setting tidak diizinkan.','VALIDATION_ERROR');var value=input.value;if(definition.Type==='BOOLEAN'){if(value!==true&&value!==false&&String(value)!=='true'&&String(value)!=='false')throw new AppError('Nilai harus boolean.','VALIDATION_ERROR');value=String(value)==='true'||value===true;}else if(definition.Type==='NUMBER'){value=Number(value);if(!isFinite(value)||value<0)throw new AppError('Nilai harus angka non-negatif.','VALIDATION_ERROR');}else value=rejectFormula_(value,500,'Value');DatabaseService.updateById('Settings','Key',key,{Value:value,UpdatedAt:nowIso_()});invalidateSettingsCache_();audit_(auth,'ADMIN_UPDATE_SETTING','Setting',key,'Memperbarui setting');return successResponse({Key:key,Value:value},'Setting berhasil diperbarui.');}
  function admins(token,input){access_(token,'SETTINGS',true);return successResponse(page_(DatabaseService.getAllRows('Users').filter(function(x){return x.Role==='ADMIN'||x.Role==='SUPERADMIN';}).map(function(x){return{UserID:x.UserID,Name:x.Name,Email:x.Email,Role:x.Role,Status:x.Status,LastLogin:x.LastLogin,CreatedAt:x.CreatedAt};}),input,['CreatedAt','Name','Email','Role','Status','LastLogin']),'Admin berhasil dimuat.');}
  function createAdmin(token,input){var auth=access_(token,'SETTINGS',true),email=sanitizeText_(input.email,160,'Email').toLowerCase(),role=sanitizeText_(input.role,20,'Role'),password=String(input.password||'');ValidationService.validateEnum(role,['ADMIN','SUPERADMIN'],'Role');ValidationService.validateEmail(email);if(password.length<8||password.length>128)throw new AppError('Password harus 8-128 karakter.','VALIDATION_ERROR');if(DatabaseService.getAllRows('Users').some(function(x){return String(x.Email).toLowerCase()===email;}))throw new AppError('Email sudah terdaftar.','DUPLICATE_EMAIL');var salt=generateSalt(),now=nowIso_(),row={UserID:generateId('USR'),Name:rejectFormula_(input.name,120,'Nama'),NIS:'',SchoolID:'',Class:'',BirthDate:'',WhatsApp:'',Email:email,PasswordHash:hashPassword(password,salt),PasswordSalt:salt,Role:role,Status:'ACTIVE',TotalPointCache:0,FraudScore:0,CreatedAt:now,UpdatedAt:now,LastLogin:''};DatabaseService.insert('Users',row);audit_(auth,'ADMIN_CREATE_ADMIN','User',row.UserID,'Membuat '+role);return successResponse({UserID:row.UserID,Name:row.Name,Email:row.Email,Role:row.Role,Status:row.Status},'Admin berhasil dibuat.');}
  function updateAdmin(token,input){var auth=access_(token,'SETTINGS',true),id=sanitizeText_(input.userId,40,'UserID'),u=DatabaseService.findById('Users','UserID',id);if(!u||['ADMIN','SUPERADMIN'].indexOf(u.Role)<0)throw new AppError('Admin tidak ditemukan.','NOT_FOUND');var role=input.role||u.Role,status=input.status||u.Status;ValidationService.validateEnum(role,['ADMIN','SUPERADMIN'],'Role');ValidationService.validateEnum(status,DATABASE_ENUMS.UserStatus,'Status');if(u.Role==='SUPERADMIN'&&(role!=='SUPERADMIN'||status!=='ACTIVE')){var active=DatabaseService.getAllRows('Users').filter(function(x){return x.Role==='SUPERADMIN'&&x.Status==='ACTIVE';});if(active.length<=1)throw new AppError('SUPERADMIN aktif terakhir tidak dapat dinonaktifkan atau diturunkan.','LAST_SUPERADMIN');}DatabaseService.updateById('Users','UserID',id,{Role:role,Status:status,UpdatedAt:nowIso_()});audit_(auth,'ADMIN_UPDATE_ADMIN','User',id,'Mengubah role/status admin');return successResponse({UserID:id,Role:role,Status:status},'Admin berhasil diperbarui.');}
  function exportReport(token,input){access_(token,'REPORTS');var types={participants:['Users','CreatedAt'],schools:['Schools','CreatedAt'],seasons:['Seasons','CreatedAt'],quiz:['QuizSessions','CreatedAt'],leaderboard:['PointTransactions','CreatedAt'],winner:['Winners','SelectedAt'],reward:['Rewards','CreatedAt'],point:['PointTransactions','CreatedAt'],fraud:['FraudLogs','CreatedAt']},type=sanitizeText_(input.type,30,'Jenis'),cfg=types[type];if(!cfg)throw new AppError('Jenis laporan tidak valid.','VALIDATION_ERROR');var rows=DatabaseService.getAllRows(cfg[0]),filters=input.filters||{};rows=rows.filter(function(r){return(!filters.seasonId||r.SeasonID===filters.seasonId)&&(!filters.schoolId||r.SchoolID===filters.schoolId)&&(!filters.startDate||String(r[cfg[1]])>=filters.startDate)&&(!filters.endDate||String(r[cfg[1]]).slice(0,10)<=filters.endDate);});if(rows.length>5000)throw new AppError('Hasil ekspor terlalu besar. Persempit filter.','EXPORT_LIMIT');var headers=DATABASE_SCHEMA[cfg[0]].filter(function(h){return['PasswordHash','PasswordSalt','QuestionIDs','DestinationNumber'].indexOf(h)<0;}),lines=[headers.join(',')].concat(rows.map(function(r){return headers.map(function(h){return csv_(r[h]);}).join(',');}));return successResponse({filename:'pemilih-muda-'+type+'-'+String(nowIso_()).slice(0,10)+'.csv',csv:'\ufeff'+lines.join('\r\n'),count:rows.length},'Laporan berhasil dibuat.');}
  function csv_(v){return'"'+String(v===undefined?'':v).replace(/"/g,'""')+'"';}
  return Object.freeze({dashboard:dashboard,users:users,userDetail:userDetail,updateUser:updateUser,userStatus:userStatus,resetPassword:resetPassword,schools:schools,saveSchool:saveSchool,schoolStatus:schoolStatus,seasons:seasons,updateSeason:updateSeason,seasonStatus:seasonStatus,duplicateSeason:duplicateSeason,questions:questions,updateQuestion:updateQuestion,questionStatus:questionStatus,quizSessions:quizSessions,adjustPoint:adjustPoint,contentList:contentList,saveContent:saveContent,fraud:fraud,reviewFraud:reviewFraud,activity:activity,rewards:rewards,settings:settings,updateSetting:updateSetting,admins:admins,createAdmin:createAdmin,updateAdmin:updateAdmin,exportReport:exportReport,validateSeasonTransition:validateTransition_});
})();
